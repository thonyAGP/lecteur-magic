```json
{
  "programId": 155,
  "programName": "Controle fermeture caisse WS",
  "coveragePct": 42,
  "rulesImplemented": 1,
  "rulesTotal": 6,
  "missingRules": [
    "RM-004: Condition: Param K/T [A] egale 'T' - NOT IMPLEMENTED in store logic",
    "RM-005: Si VG78 alors Val([BO] sinon '3')=p.i.Terminal coffre2 - VG78 variable logic MISSING",
    "RM-006: Negation de VG78 - Inverse condition NOT IMPLEMENTED",
    "RM-001: Param UNI/BI='B' - ONLY UI binding exists, no business rule enforcement",
    "RM-002: Complex 2-caisses condition - PARTIAL implementation, missing hostCourant/sessionOuverte checks"
  ],
  "recommendations": [
    "CRITICAL: Add business rule validation in store actions (validerParametresUniBi should enforce RM-001)",
    "CRITICAL: Implement RM-004 (Param K/T='T' branch) - currently only 'K' case is tested",
    "CRITICAL: Add VG78 global variable logic (RM-005/RM-006) for coffre2 terminal/hostname configuration",
    "HIGH: Missing API endpoints for callees IDE 135 (tableau recap) and IDE 142 (maj devises session)",
    "HIGH: Missing table entities: pointage_devise, pointage_article, pointage_appro_remise, histo_sessions_caisse, pv_comptable, pv_discounts",
    "MEDIUM: ValidationParametresPanel only renders UI fields but doesn't call validation actions on change",
    "MEDIUM: GnrationRcapitulatifPanel uses wrong API endpoint path (/api/controle-fermeture-caisse/ instead of /api/caisse/fermeture/)",
    "MEDIUM: CalculEcartsPanel missing getClasseMoyenPaiement integration (should populate classeMoyenPaiement/libelleMoyenPaiement)",
    "LOW: PointagesFinauxPanel doesn't call backend to fetch pointages data (static UI)",
    "LOW: FinalisationPanel should validate 21 tables were updated correctly (contract specifies 7 RW + 2 W + 12 R tables)",
    "ARCHITECTURE: Store mixes UI state (sessionId, isLoading) with domain state - consider separating concerns",
    "ARCHITECTURE: Error handling inconsistent - some actions use setError, others throw exceptions",
    "TESTING: No unit tests for store actions - only approTicketStore.test.ts exists",
    "TESTING: Missing E2E tests for 5-step workflow (params -> recap -> ecarts -> pointages -> final)",
    "DOCUMENTATION: Missing JSDoc for complex business rules (especially RM-002 composite condition)"
  ]
}
```

**Détail des gaps critiques:**

1. **Règles métier non implémentées (50% manquant):**
   - `RM-004`: Logique Param K/T='T' absente
   - `RM-005/RM-006`: Variable globale VG78 jamais utilisée
   - `RM-001/RM-002`: Validation UI uniquement, pas d'enforcement côté store

2. **Tables contract vs code:**
   - Contract: 21 tables (7 RW + 2 W + 12 R)
   - Code: 3 entities définies (PointageDevise, PointageArticle, HistoSessionsCaisse)
   - **Manque 18 tables**

3. **API endpoints:**
   - Définis: 7 endpoints dans controleFermetureCaisseApi
   - **Manque: 2 callees critiques** (IDE 135 génération recap, IDE 142 maj devises)

4. **Validation métier:**
   - UI binding existe pour tous les paramètres
   - **Actions de validation côté store non implémentées**
   - Exemple: `validerParametresUniBi()` retourne `Promise<boolean>` mais logique métier absente