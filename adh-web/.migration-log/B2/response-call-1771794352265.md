```json
{
  "programId": 125,
  "programName": "Remise en caisse",
  "coveragePct": 45,
  "rulesImplemented": 0,
  "rulesTotal": 1,
  "missingRules": [
    "RM-001: Condition 'Param quand [D]<>'P'' - Store does not implement conditional logic based on quand parameter being different from 'P'. The store accepts quand as 'O' | 'F' but doesn't check for 'P' exclusion.",
    "Table pv_comptable (505) - Not represented in types or store. Missing PV comptable generation for anomalies/ecarts.",
    "Table gestion_devise_session (232) - Partial coverage. Missing columns: EO (Param devise locale), ES (Nbre devises), EX (Param Nbre devise comptees), FE (Param Mt versement Nb devises).",
    "Table saisie_approvisionnement (244) - Not represented in types or store. Missing approvisionnement tracking logic.",
    "Table gestion_article_session (196) - Not represented in types or store. Missing article/stock management.",
    "Table email_reprise (501) - Not represented in types or store. Missing email notification logic.",
    "CallTask to ADH IDE 43 (get member title) - Not implemented. Missing member context retrieval.",
    "CallTask to ADH IDE 142 (WebSocket session update) - Not implemented. Missing real-time session updates.",
    "CallTask to ADH IDE 149 (stock calculation) - Not implemented. Missing stock consistency validation.",
    "Resort Credit display - Implemented in ResortCreditDisplayPanel but not wired to main flow as per spec context.",
    "PV comptable generation - Missing logic to generate comptability report (pv_comptable) when ecarts detected.",
    "Stock validation - Missing validation that articles remis match stock disponible.",
    "Email notification - Missing email_reprise table updates and notification triggering."
  ],
  "recommendations": [
    "CRITICAL: Implement RM-001 rule - Add conditional logic in initComptage() to check if quand !== 'P' before allowing comptage initialization.",
    "HIGH: Add PvComptable entity type and store methods to generate comptability reports when recapMOP shows ecarts !== 0.",
    "HIGH: Add missing columns to DeviseComptage interface: deviseLocaleParam, nbreDevises, nbreDeviseComptees, mtVersementNbDevises to match table 232.",
    "HIGH: Create ApprovisionnementTracking entity and store methods to update saisie_approvisionnement table (244).",
    "MEDIUM: Create ArticleSession entity and store methods to manage gestion_article_session table (196).",
    "MEDIUM: Add email notification logic - create EmailReprise entity and trigger email on comptage persistence.",
    "MEDIUM: Integrate ADH IDE 43 call to fetch member title (Titre) and display in HeaderPanel.",
    "MEDIUM: Implement WebSocket integration with ADH IDE 142 for real-time session updates during comptage.",
    "MEDIUM: Add stock validation call to ADH IDE 149 before allowing persistComptage() to ensure stock coherence.",
    "LOW: Wire ResortCreditDisplayPanel into main SaisieContenuCaissePage flow as contextual information panel.",
    "LOW: Add validation in validateComptage() to prevent processing when quand === 'P' (per RM-001).",
    "CODE QUALITY: Add unit tests for conditional logic (quand parameter validation).",
    "CODE QUALITY: Add integration tests for PV comptable generation scenario.",
    "DOCUMENTATION: Update types/saisieContenuCaisse.ts with JSDoc comments explaining the quand parameter values and their business meaning."
  ]
}
```